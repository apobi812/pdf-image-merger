{
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check sw.js && node --check worker/src/index.js && node --check worker/generate-admin-secret.mjs && node tools/verify-release.mjs"
  }
}
